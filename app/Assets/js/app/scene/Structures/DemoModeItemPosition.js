export default class DemoModeItemPosition {
    constructor(initX, initY, finalX, finalY) {
        this.initial = {
            x: initX,
            y: initY
        };
        this.final = {
            x: finalX,
            y: finalY
        };
    }
};